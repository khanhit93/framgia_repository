<%= form_for [:admin, @category] do |f| %>
  <%= render "shared/error_messages", object: f.object  %>
  <%= f.label :name %>
  <%= f.text_field :conten, class: "form-control" %>
  <div class="buton_action">
    <%= f.submit class: "my_btn" %>
  </div>
<% end %>
