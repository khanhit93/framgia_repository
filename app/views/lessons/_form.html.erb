<%= form_for @lesson do |f| %>
  <%= f.select :category_id,
    options_for_select(@categories.map{|c| [c.conten, c.id]}),
      {}, class: "form-control" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
